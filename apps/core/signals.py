"""
Django signals for the EduOS core application.

Register custom signals here (e.g. audit-log triggers, cross-app
domain events).  Connect handlers in ``apps.core.apps.CoreConfig.ready()``
or via the ``@receiver`` decorator.
"""

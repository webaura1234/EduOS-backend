"""
Analytics — operational logs.

Groups all append-only log/event models:
  - AuditLog       → immutable record of every sensitive action (who did what, when)
  - SupportModeLog → tracks when support staff enter a tenant's data in support mode
  - JobRun         → records of scheduled/async background job executions
"""

# TODO: Implement models in this file.
# Suggested models: AuditLog, SupportModeLog, JobRun
